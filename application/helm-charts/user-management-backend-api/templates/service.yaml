apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-management-backend-api.fullname" . }}
  labels:
    {{- include "user-management-backend-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-management-backend-api.selectorLabels" . | nindent 4 }}
