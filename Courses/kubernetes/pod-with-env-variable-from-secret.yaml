apiVersion: v1
kind: Pod
metadata:
  name: env-configmap
  namespace: test
spec:
  containers:
    - name: app
      command: ["/bin/sh", "-c", "printenv"]
      image: busybox:latest
      envFrom:
        - configMapRef:
            name: web-config